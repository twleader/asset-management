# 環境變數（含密碼，勿上傳）
.env

# macOS
.DS_Store

# Java build output
backend/target/
bff/target/
external-materials-service/target/

# Node modules
frontend/node_modules/
frontend/dist/

# IDE
.idea/
*.iml
.vscode/

# Claude Code 本機設定備份（含本機路徑 / 允許查詢清單，勿上傳）
.claude.bak/

# 資料庫檔與資料 dump（含真實個人財務資料，勿上傳 — 資安 Requirement 29）
# 生產環境的 seed / 個資改由部署方本機提供；如需保留「不含真實資料」的種子檔，請用 git add -f
#
# ⚠ 排除範圍限定在 db/init/ 之下，**請勿放寬成 db/*.sql**：
#   db/schema.sql 是刻意納入版控的 schema-only 鏡像（純結構、零資料），
#   供稽核比對 Entity / Liquibase / 實際 DB 三者是否一致，放寬規則會把它一併排除。
*.mv.db
*.trace.db
/data/
backend/data/
db/init/*.sql
stock_alert_data.sql
